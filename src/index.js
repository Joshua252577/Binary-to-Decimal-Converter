const bin = document.querySelector('.binario').value;
const dec = document.querySelector('.decimal');
const btn = document.querySelector('.btn');

btn.addEventListener('Click', function converter(event, bin) {
    event.preventDefault();
    console.log('clicou')
    
});